export const Config = {
    // Aapka Spring Boot ka base URL
    // Agar aapka backend 8080 par chal raha hai to:
    CORE_API: "http://localhost:8080/api"
    
    // Agar future mein kisi aur port ya server par jaye to sirf yahan change hoga
    // CORE_API: "http://localhost:8020/api" 
};